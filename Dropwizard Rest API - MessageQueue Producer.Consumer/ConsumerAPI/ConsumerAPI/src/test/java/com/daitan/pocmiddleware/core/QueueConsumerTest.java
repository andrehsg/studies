package com.daitan.pocmiddleware.core;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import java.net.URI;
import java.net.URISyntaxException;

public class QueueConsumerTest {

  /*
  @Test
  public void testReadQueue() throws URISyntaxException {

    AmazonQueueService amazonService = new AmazonQueueService(new URI("http://localhost:4576"));
    amazonService.createQueue("testReadQueue");
    QueueConsumer queueConsumer = new QueueConsumer().readQueue("http://localhost:4576/queue/testReadQueue");
    assertEquals(true,true);
  }

   */

}
